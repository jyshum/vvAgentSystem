OVERHAUL:
1. Scope down to strictly the admin page
2. Remove users as a page from the admin dashboard
4. Strip down the dashboard by keeping features simple as possible
    a. Config page: Running the AI visibility tracker (setting the queries, the domain, everything needed to run the tracker in VS code --> everything from childspot.json)