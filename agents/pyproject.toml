[project]
name = "geo-tracker"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "openai>=1.82.0",
    "anthropic>=0.52.0",
    "google-genai>=1.14.0",
    "python-dotenv>=1.0.0",
    "supabase>=2.0.0",
]

[project.optional-dependencies]
dev = ["pytest>=8.0.0"]
